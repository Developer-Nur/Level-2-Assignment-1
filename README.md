TypeScript: Interfaces নাকি Types? আর keyof-এর জাদু!

TypeScript-এ কাজ করার সময় একটা প্রশ্ন প্রায়ই আসে – Interface ব্যবহার করব নাকি Type Alias? দুটোই আমাদের কোডে ডেটার গঠন (structure) কেমন হবে তা বোঝাতে সাহায্য করে, কিন্তু এদের মধ্যে কিছু গুরুত্বপূর্ণ পার্থক্য আছে যা জানা খুব জরুরি। এছাড়াও, TypeScript-এর একটা দারুণ সুবিধা হলো keyof অপারেটর। এই ব্লগ পোস্টে আমরা এই তিনটি বিষয় নিয়েই সহজভাবে আলোচনা করব।

💡 Interfaces vs. Types: মূল পার্থক্যগুলো কী কী?
যদিও Interface এবং Type Alias-এর ব্যবহার অনেকক্ষেত্রে একই রকম মনে হতে পারে, এদের কিছু মৌলিক তফাৎ আছে যা এদেরকে ভিন্ন পরিস্থিতিতে উপযোগী করে তোলে:

১. extends এবং implements-এর ব্যবহার
Interfaces: Interface-গুলো অন্য Interface বা Class-কে extend করতে পারে এবং Class-গুলো Interface-কে implement করতে পারে। এটাই Interface-এর অন্যতম প্রধান কাজ।

TypeScript

interface Person {
name: string;
}

// Interface অন্য Interface-কে extend করে
interface Employee extends Person {
employeeId: number;
}

// Class Interface-কে implement করে
class Manager implements Employee {
name: string = "Rana";
employeeId: number = 101;
}
Type Aliases: Type Alias অন্য Type-কে extend করতে পারে না, কিন্তু Intersection Types (&) ব্যবহার করে আমরা অনেকটা একই কাজ করতে পারি। Class Type Alias-কে implement করতে পারে না।

TypeScript

type PersonType = {
name: string;
}

// Intersection Type (&) ব্যবহার করে
type EmployeeType = PersonType & {
employeeId: number;
}
২. Declaration Merging (একত্রীকরণ)
Interfaces: Interface-এর সবচেয়ে বড় সুবিধা হলো Declaration Merging। আপনি একই নামে একাধিক Interface ঘোষণা করতে পারেন, এবং TypeScript স্বয়ংক্রিয়ভাবে সেগুলোকে একটি Interface-এ মিশিয়ে দেবে।

TypeScript

interface Settings {
theme: 'light' | 'dark';
}

// TypeScript এই দুটোকে মিশিয়ে দেবে
interface Settings {
fontSize: number;
}

// এখন Settings-এর মধ্যে দুটো property-ই আছে
const userSettings: Settings = { theme: 'dark', fontSize: 16 };
Type Aliases: Type Alias-এ Declaration Merging সম্ভব নয়। একই নামে একাধিক Type Alias ঘোষণা করলে TypeScript এরর (Error) দেবে।

৩. কী কী গঠন করা যায়?
Type Aliases: Type Alias খুবই শক্তিশালী। এটি শুধুমাত্র Object-এর গঠন নয়, বরং আরও জটিল ডেটা টাইপ তৈরি করতে পারে:

Primitive Types (যেমন type ID = string)

Union Types (যেমন type Status = 'active' | 'inactive')

Tuple Types

Mapped Types

Conditional Types

🔑 keyof Keyword-এর ব্যবহার এবং উদাহরণ
keyof হলো TypeScript-এর একটি খুব দরকারি অপারেটর যা দিয়ে আপনি একটি Object Type-এর সমস্ত Property Name-এর Union Type বের করতে পারেন। সহজ কথায়, keyof আপনাকে বলে দেয় একটি Object-এ কী কী 'চাবি' বা কী কী Property আছে।

🤔 keyof-এর কাজ কী?
যখন আপনি এমন একটি ফাংশন তৈরি করতে চান যা একটি Object নেবে এবং তার যেকোনো একটি Property-এর মান অ্যাক্সেস করবে, তখন keyof খুব কাজে আসে। এটি নিশ্চিত করে যে আপনি একটি বৈধ (Valid) Property Name ব্যবহার করছেন।

💡 উদাহরণ: Safe Property Getter Function
ধরা যাক, আমাদের একটি User টাইপ আছে এবং আমরা এমন একটি ফাংশন লিখতে চাই যা শুধুমাত্র বৈধ Key ব্যবহার করে এই Object থেকে ডেটা নিতে পারে।

TypeScript

type User = {
id: number;
name: string;
email: string;
isAdmin: boolean;
};

// keyof User দেবে: 'id' | 'name' | 'email' | 'isAdmin'
type UserKeys = keyof User;
// UserKeys এখন একটি Union Type: 'id' | 'name' | 'email' | 'isAdmin'

// ফাংশন যা User Object এবং User-এর যেকোনো একটি key নেয়
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
return obj[key];
}

const currentUser: User = {
id: 1,
name: "Arif Khan",
email: "arif@example.com",
isAdmin: false,
};

// ✅ কাজ করবে, কারণ 'name' হলো একটি বৈধ key
const userName = getProperty(currentUser, 'name');
console.log(userName); // Output: Arif Khan

// ❌ কাজ করবে না, কারণ 'age' User-এর key নয় (TypeScript Error দেবে)
// const userAge = getProperty(currentUser, 'age');

এই উদাহরণে, K extends keyof T অংশটি TypeScript-কে জোর করে যে key আর্গুমেন্টটি অবশ্যই T (এই ক্ষেত্রে User) Object-এর একটি বৈধ Key হতে হবে। এর ফলে কোডে কম্পাইল-টাইমেই ভুল ধরার সুযোগ তৈরি হয় এবং কোড আরও সুরক্ষিত (safe) হয়।

🚀 সংক্ষিপ্ত সিদ্ধান্ত
যদি আপনি Object-এর গঠন বর্ণনা করতে চান এবং Declaration Merging বা Class Implementation আপনার প্রয়োজন হয়, তবে Interfaces ব্যবহার করুন।

যদি আপনি Union Types, Primitive Types সহ আরও জটিল ডেটা টাইপ তৈরি করতে চান বা Intersection Types ব্যবহার করে নতুন টাইপ বানাতে চান, তবে Type Aliases ব্যবহার করুন।

Object-এর Property Name-কে Type হিসাবে ব্যবহার করার প্রয়োজন হলে, keyof অপারেটর ব্যবহার করুন।

কোড পরিষ্কার, ত্রুটিমুক্ত এবং সহজে রক্ষণাবেক্ষণযোগ্য (maintainable) করার জন্য Interface, Type Alias, এবং keyof— এই তিনটিই TypeScript-এর অত্যন্ত গুরুত্বপূর্ণ টুল।
